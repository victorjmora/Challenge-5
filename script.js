// name the var you are going to use
const rowTimeBlockPast = document.getElementById();
const rowTimeBlockPresent = documentgetElementById();
const rowTimeBlockFuture = documentgetElementById();
const description = documentgetElementById();

if (hour < currentHour) {
    timeBlock.addClass("past");
} else if(hour === currentHour) {
    timeBlock.addClass("present");
} else {
    timeBlock.addClass("future")
}